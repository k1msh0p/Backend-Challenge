<h1>New Member</h1>

<%= form_for @member do |f| %>
	<% if @member.errors.any? %>
    <div id="error_explanation">
      <h3><%= pluralize(@member.errors.count, "error") %> prohibited this blog from being saved:</h3>

      <ul>
      <% @member.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
	<% end %>

	<br>
	<div class="field">
	  <label class="label">Name</label>
	  <div class="control">
	    <%= f.text_field :name, class: "input", placeholder: "name" %>
	  </div>
	</div>
	<div class="field">
	  <label class="label">Website URL</label>
	  <div class="control">
	    <%= f.text_field :website, class: "input", placeholder: "url" %>
	  </div>
	</div>
	<div class="field">
		<button class="button is-dark">Save</button>
	</div>

<% end -%>

